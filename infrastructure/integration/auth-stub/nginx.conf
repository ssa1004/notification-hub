events {}

http {
    server {
        listen 8080;

        location /.well-known/jwks.json {
            default_type application/json;
            alias /usr/share/nginx/html/jwks.json;
        }

        location /healthz {
            return 200 "ok\n";
        }
    }
}
